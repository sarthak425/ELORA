import React from 'react'

const Badge = ({ title }) => {
    return (
        <span className='py-2 px-3 text-xs text-amber-900 bg-EveningBlack rounded-full'>{title}</span>
    )
}

export default Badge
